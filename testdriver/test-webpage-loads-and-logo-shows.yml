version: 4.1.13
steps:
  - prompt: Assert that the webpage loads successfully.
    commands:
      - command: assert
        expect: Disclaimer text is visible on the webpage
  - prompt: >-
      Assert that the logo is visible on the page.  # Use the content of the
      file from the environment
    commands:
      - command: assert
        expect: ASSET text is visible on the webpage
